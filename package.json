{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "MERN team project and task manager with JWT auth, project roles, REST APIs, and MongoDB.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "railway:start": "npm run start -w server",
    "db:seed": "npm run seed -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
